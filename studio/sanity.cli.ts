import { defineCliConfig } from 'sanity/cli'

export default defineCliConfig({
    api: {
        projectId: '2saavkiq',
        dataset: 'production'
    },
    studioHost: 'manisha-sharma-mentoria'
})
